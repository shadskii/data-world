
[build.environment]
  NODE_VERSION = "16"
  NPM_FLAGS = "--version" # prevent Netlify npm install

[build]
  publish = "dist"
  command = "cd frontend && npx pnpm i --store=node_modules/.pnpm-store && npm run build"